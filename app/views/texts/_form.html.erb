<%= simple_form_for [@text, @modification] do |f| %>
  <%= f.input :content_before, as: :hidden %>
  <%= f.input :content_after, as: :hidden %>
<% end %>

<button id="saveBtn">Save</button>

<div class="d-none" id="textContent" data-json="<%= @content.to_json %>"></div>

<div id="editorjs"></div>
